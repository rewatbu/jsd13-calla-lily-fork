# setting up
- use `npm install` in the /backend and /frontend directory to get required packages installed
- use the `npm run seed` to connect to the database of your choice and create the initial dataset
